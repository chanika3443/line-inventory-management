# LINE Inventory Management System

ระบบจัดการวัสดุคงคลังผ่าน LINE Official Account

## โครงสร้างไฟล์

```
src/
├── Code.gs              # Main entry point (doGet, doPost, LINE webhook)
├── SheetService.gs      # Data layer for Google Sheets operations
├── ProductService.gs    # Business logic for product management
├── TransactionService.gs # Business logic for transactions
├── DashboardService.gs  # Dashboard and report services
├── Setup.gs             # Setup and initialization scripts
├── base.html            # Base CSS styles (included by other pages)
├── index.html           # Main menu page
├── products.html        # Product management page
├── withdraw.html        # Withdrawal page
├── receive.html         # Receipt page
├── dashboard.html       # Dashboard page
└── reports.html         # Reports and logs page
```

## การติดตั้ง

### 1. สร้าง Google Sheets และ Apps Script

1. สร้าง Google Sheets ใหม่
2. ไปที่ Extensions > Apps Script
3. สร้างไฟล์ทั้งหมดใน Apps Script:
   - กด **+** > **สคริปต์** สำหรับไฟล์ `.gs`
   - กด **+** > **HTML** สำหรับไฟล์ `.html`
4. Copy เนื้อหาจากแต่ละไฟล์ใน `src/` ไปวาง

> ⚠️ **หมายเหตุ:** Apps Script ไม่รองรับ folder structure ต้องสร้างไฟล์แบบ flat ทั้งหมด

### 2. ตั้งค่า Sheets

รันฟังก์ชัน `initializeSheets()` จาก Apps Script editor:
1. เลือกไฟล์ `Setup.gs`
2. เลือกฟังก์ชัน `initializeSheets`
3. กด Run

หรือหลังจาก deploy แล้ว ใช้เมนู **📦 ระบบคลังวัสดุ** > **🔧 ตั้งค่าระบบ**

### 3. Deploy Web App

1. ไปที่ **Deploy** > **New deployment**
2. เลือก Type: **Web app**
3. ตั้งค่า:
   - Execute as: **Me**
   - Who has access: **Anyone**
4. กด **Deploy** และคัดลอก Web App URL

### 4. ตั้งค่า LINE (Optional)

#### 4.1 สร้าง Messaging API Channel (สำหรับ Webhook)
1. ไปที่ [LINE Developers Console](https://developers.line.biz/console/)
2. สร้าง Provider (ถ้ายังไม่มี)
3. สร้าง **Messaging API** channel
4. ไปที่แท็บ **Messaging API**:
   - คัดลอก **Channel access token** (กด Issue ถ้ายังไม่มี)
   - ตั้งค่า **Webhook URL** เป็น Web App URL
   - เปิด **Use webhook**

#### 4.2 สร้าง LINE Login Channel (สำหรับ LIFF)
> ⚠️ **สำคัญ:** LIFF ต้องสร้างใน LINE Login channel เท่านั้น ไม่ใช่ Messaging API channel

1. ใน Provider เดียวกัน สร้าง **LINE Login** channel ใหม่
2. ไปที่แท็บ **LIFF**
3. กด **Add** เพื่อสร้าง LIFF app:
   - **LIFF app name**: ระบบคลังวัสดุ
   - **Size**: Full
   - **Endpoint URL**: ใส่ Web App URL ที่ได้จาก Apps Script
   - **Scopes**: เลือก `profile`
4. กด **Add** แล้วคัดลอก **LIFF ID** (เช่น `1234567890-AbcdEfgh`)

#### 4.3 เพิ่ม Script Properties
1. ใน Apps Script ไปที่ **⚙️ Project Settings** (ไอคอนเฟืองด้านซ้าย)
2. เลื่อนลงไปที่ **Script Properties**
3. กด **Add script property** เพิ่ม:
   - `LINE_CHANNEL_ACCESS_TOKEN`: Channel access token จาก Messaging API
   - `LIFF_ID`: LIFF ID จาก LINE Login channel

## Google Sheets Structure

ระบบจะสร้าง 3 sheets อัตโนมัติเมื่อรัน `initializeSheets()`:

### Products Sheet
| Column | Field | Description |
|--------|-------|-------------|
| A | code | รหัสวัสดุ (unique) |
| B | name | ชื่อวัสดุ |
| C | unit | หน่วย |
| D | quantity | จำนวนคงเหลือ |
| E | lowStockThreshold | จุดเตือนวัสดุใกล้หมด |
| F | category | หมวดหมู่ |
| G | createdAt | วันที่สร้าง |
| H | updatedAt | วันที่แก้ไขล่าสุด |

### Transactions Sheet
| Column | Field | Description |
|--------|-------|-------------|
| A | id | รหัสรายการ |
| B | timestamp | วันเวลาที่ทำรายการ |
| C | type | ประเภท (WITHDRAW/RECEIVE/EDIT/DELETE/CREATE) |
| D | productCode | รหัสวัสดุ |
| E | productName | ชื่อวัสดุ |
| F | quantity | จำนวนที่ทำรายการ |
| G | beforeQuantity | จำนวนก่อนทำรายการ |
| H | afterQuantity | จำนวนหลังทำรายการ |
| I | userName | ชื่อผู้ทำรายการ |
| J | note | หมายเหตุ |

### Settings Sheet
| Column | Field | Description |
|--------|-------|-------------|
| A | key | ชื่อการตั้งค่า |
| B | value | ค่า |

## API Endpoints

### GET Endpoints
| Endpoint | Description |
|----------|-------------|
| `?page=products` | หน้าจัดการวัสดุ |
| `?page=withdraw` | หน้าเบิกวัสดุ |
| `?page=receive` | หน้ารับเข้าวัสดุ |
| `?page=dashboard` | หน้า Dashboard |
| `?page=reports` | หน้ารายงานและประวัติ |
| `?action=getProducts` | ดึงวัสดุทั้งหมด |
| `?action=getProduct&code=XXX` | ดึงวัสดุตามรหัส |
| `?action=searchProducts&query=XXX` | ค้นหาวัสดุ |
| `?action=getLowStock` | ดึงวัสดุใกล้หมด |
| `?action=getNextCode` | ดึงรหัสวัสดุถัดไป |
| `?action=getDashboard` | ดึงข้อมูล Dashboard |
| `?action=getLogs` | ดึงประวัติการทำรายการ (รองรับ filters) |
| `?action=getReport` | ดึงข้อมูลรายงาน (รองรับ filters) |

### POST Endpoints
```json
// เพิ่มวัสดุ
{ "action": "addProduct", "product": {...} }
// หรือ flat structure
{ "action": "addProduct", "name": "XXX", "unit": "XXX", "quantity": 10, "threshold": 5, "category": "XXX", "userName": "XXX" }

// แก้ไขวัสดุ
{ "action": "updateProduct", "code": "XXX", "updates": {...} }
// หรือ flat structure
{ "action": "updateProduct", "code": "XXX", "name": "XXX", "unit": "XXX", "quantity": 10, "threshold": 5, "category": "XXX", "userName": "XXX" }

// ลบวัสดุ
{ "action": "deleteProduct", "code": "XXX", "userName": "XXX" }

// เบิกวัสดุ
{ "action": "withdraw", "productCode": "XXX", "quantity": 10, "userName": "XXX" }

// รับเข้าวัสดุ
{ "action": "receive", "productCode": "XXX", "quantity": 10, "userName": "XXX" }

// เบิกวัสดุหลายรายการ
{ "action": "batchWithdraw", "items": [{"productCode": "XXX", "quantity": 10}, ...], "userName": "XXX" }
```

## คุณสมบัติหลัก

### จัดการวัสดุ
- เพิ่ม/แก้ไข/ลบวัสดุ
- สร้างรหัสวัสดุอัตโนมัติ (P0001, P0002, ...)
- ค้นหาวัสดุตามชื่อหรือรหัส
- ตรวจสอบวัสดุใกล้หมด (Low Stock Alert)
- จัดหมวดหมู่วัสดุ

### เบิก/รับเข้าวัสดุ
- เบิกวัสดุออกจากคลัง
- รับเข้าวัสดุเข้าคลัง
- เบิกวัสดุหลายรายการพร้อมกัน (Batch Withdraw)
- ตรวจสอบจำนวนคงเหลือก่อนเบิก
- บันทึกชื่อผู้ทำรายการ

### Dashboard และรายงาน
- แสดงจำนวนวัสดุทั้งหมด
- แสดงจำนวนวัสดุใกล้หมด
- แสดงรายการวัสดุใกล้หมด
- ประวัติการทำรายการ (Logs)
- รายงานสรุปการเบิก/รับเข้า
- กรองข้อมูลตามวันที่, ประเภท, วัสดุ, ผู้ทำรายการ

### LINE Integration
- เมนู Flex Message แบบ Carousel
- เปิดหน้าต่างๆ ผ่าน LINE
- รองรับ LIFF (LINE Front-end Framework)

## ฟังก์ชันที่มีประโยชน์

| ฟังก์ชัน | Description |
|----------|-------------|
| `initializeSheets()` | สร้าง sheets พร้อม headers |
| `addSampleProducts()` | เพิ่มวัสดุตัวอย่าง 5 รายการ |
| `clearAllData()` | ลบข้อมูลทั้งหมด (เก็บ headers) |
| `getWebAppUrl()` | แสดง Web App URL |

## License

MIT
